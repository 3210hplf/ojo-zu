<div class="flex flex-col items-center justify-center min-h-screen bg-gray-100">
  <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-4xl">
    <h1 class="text-2xl font-bold text-center mb-4">作品一覧</h1>
    <% if @posts.present? %>
      <div class="grid grid-cols-3 sm:grid-cols-3 md:grid-cols-4 gap-5">
        <% @posts.each do |post| %>
          <%= link_to post_path(post) do %>
            <div class="w-full aspect-w-1 aspect-h-1">
              <%= image_tag post.image.url, class: "w-full h-full object-cover rounded-lg shadow-xl", alt: "作品" %>
            </div>
          <% end %>
        <% end %>
      </div>
    <% else %>
      <div class="mb-3 text-center">投稿ボタンをタップして<br>お子さまの作品を投稿しよう</div>
    <% end %>
  </div>
</div>
